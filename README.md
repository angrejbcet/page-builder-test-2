# Point.Click.Engage

Exported from [AgencyForge](https://agencyforge.io).

## Getting Started

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Build for Production

```bash
npm run build
npm start
```

Deploy to Vercel, Netlify, or any Node host. (This template uses the standard Next.js server build, not `output: "export"`.)
